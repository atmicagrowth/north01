.next/
node_modules/
pnpm-lock.yaml
next-env.d.ts

# Generated by Payload - never hand-formatted.
src/payload-types.ts
src/app/(payload)/admin/importMap.js

# Generated by `payload migrate:create` - drizzle-kit writes the snapshot and Payload rewrites
# the barrel on every generation, so formatting either one only creates a diff that the next
# migration undoes. The migration `.ts` files themselves are reviewed and formatted like code.
src/payload/migrations/*.json
src/payload/migrations/index.ts

# Markdown is hand-authored throughout this repository: the canonical specification
# documents are fixed text, and the docs/ set uses deliberate line breaks and compact
# tables that Prettier's table padding would only make noisier to diff. Line endings and
# indentation for Markdown are governed by .editorconfig instead.
*.md
